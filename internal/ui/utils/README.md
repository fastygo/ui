# `registry:utils`

Thin app helpers on top of **`github.com/fastygo/templ/utils`**. Do not duplicate CVA, tag resolution, or ARIA — delegate to templ.

Add functions here only when they are **FastyGoUI-specific** (showcase paths, registry labels, merge helpers used across blocks). Generic class/tag logic belongs in **templ/utils**.
